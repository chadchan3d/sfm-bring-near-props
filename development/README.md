# Development candidate

`BNP_T156_Columns_Rows_Grid_Labels_RC.py` is a preserved post-release development candidate dated 2026-09-17.

Relative to the source in the surviving v1.0.1 archive, it makes only the final UI-copy refinement from the T154/T155/T156 sequence:

- Grid's first direction label becomes **Columns** instead of **Across**;
- Help describes **Columns** and **Rows**;
- the redundant sentence listing the X/Y/Z names in Help is removed; and
- its diagnostic script/log identity advances from T154 to T156.

This candidate is retained because the shipped `README.txt` already uses the Columns/Rows wording. It is not substituted for the byte-identical stable release source, and no separate complete SFM runtime qualification log for T156 is preserved in the publication evidence.
